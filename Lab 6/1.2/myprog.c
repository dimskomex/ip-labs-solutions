#include <stdio.h>

// Κυρίως πρόγραμμα
int main() {
  // Δήλωση μεταβλητών
  int a = 5, b = 4, sum = 0, diff = 0;

  // Κλήση της συνάρτησης goodf με χρήση δεικτών
  goodf(a, b, &sum, &diff);

  // Εμφάνιση των αποτελεσμάτων
  printf("Sum: %d\n", sum);
  printf("Diff: %d\n", diff);

  return 0;
}

// Τώρα, η συνάρτηση goodf χρησιμοποιεί δείκτες για τις μεταβλητές sum και diff.
// Αυτό σημαίνει ότι μπορεί να τροποποιήσει απευθείας τις τιμές στις θέσεις
// μνήμης που δείχνουν οι δείκτες. Τα αποτελέσματα του sum και diff θα
// υπολογιστούν σωστά και θα εμφανιστούν στο κυρίως πρόγραμμα.

// Μεταγλώττιση:
// gcc -c myfun.c
// gcc -c myprog.c
// gcc -o myprog myfun.o myprog.o

// Εκτέλεση:
// ./myprog

// Αναμενόμενο αποτέλεσμα:
// Sum: 9
// Diff: 1

// Η χρήση δεικτών επέτρεψε την τροποποίηση των μεταβλητών sum και diff εντός
// της συνάρτησης goodf. Αυτό διορθώνει το πρόβλημα που υπήρχε στην αρχική
// έκδοση με την badf, όπου οι τιμές δεν αποθηκεύονταν.